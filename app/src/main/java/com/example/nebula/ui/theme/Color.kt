package com.example.nebula.ui.theme

import androidx.compose.ui.graphics.Color

// ── Deep Space Palette ───────────────────────────────────────────────────────
val VoidBlack    = Color(0xFF06080D)   // true void — background
val AbyssBlue    = Color(0xFF0B1020)   // secondary surface

// Nebula accent triad
val NebulaGlow   = Color(0xFF52D6F4)   // ice-cyan — like Crab Nebula plasma jets
val NebulaPurple = Color(0xFFB76CFF)   // soft violet — like Orion Nebula emission
val NebulaAmber  = Color(0xFFFFB347)   // stellar amber — like T Tauri star light

// Text
val StarWhite    = Color(0xFFEBF5FF)   // primary text — ice white with blue tint
val StarDim      = Color(0xFF3D5A73)   // secondary text — blue-gray

// Glass surfaces (used in PanelView / nav)
val GlassDark    = Color(0x14FFFFFF)   // 8% white
val GlassBorder  = Color(0x1AFFFFFF)   // 10% white border
